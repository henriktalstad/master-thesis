# Eval overlay (optional)

Optional CSV files for external energy reconciliation. Not required to run the application.

Expected files: `electricity_15min.csv`, `district_heating_15min.csv`, `manifest.json` — see file headers for columns.
